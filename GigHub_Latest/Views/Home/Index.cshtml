@model IEnumerable<GigHub_Latest.Models.Gig>
@{
    ViewBag.Title = "Home Page";
}

<ul>
    @foreach (var gig in Model)
    {
        <li>
            @gig.DateTime - @gig.Artist.Name
        </li>
    }


</ul>

